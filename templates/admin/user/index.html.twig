{% extends 'admin/admin_base.html.twig' %}
{% block body %}

    <h2>List User</h2>

    <ul>
        {% for user in users %}
            <li> {{ user.email }}, {{ user.roles | json_encode }} </li>
        {% endfor %}
    </ul>
{% endblock %}